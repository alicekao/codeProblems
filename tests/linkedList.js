var expect = require('chai').expect;
var isLinkedListCycle = require('../problems/linkedList');

describe('Linked list function', function() {
  it('should return true', function() {
    expect(isLinkedListCycle(true)).to.be.true;
  });
});
